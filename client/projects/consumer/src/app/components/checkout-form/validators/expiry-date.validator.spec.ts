import { ExpiryDate.Validator } from './expiry-date.validator';

describe('ExpiryDate.Validator', () => {
  it('should create an instance', () => {
    expect(new ExpiryDate.Validator()).toBeTruthy();
  });
});
