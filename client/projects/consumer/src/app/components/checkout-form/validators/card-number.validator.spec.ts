import { CardNumber.Validator } from './card-number.validator';

describe('CardNumber.Validator', () => {
  it('should create an instance', () => {
    expect(new CardNumber.Validator()).toBeTruthy();
  });
});
