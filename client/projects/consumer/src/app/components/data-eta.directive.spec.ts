import { DataEtaDirective } from './data-eta.directive';

describe('DataEtaDirective', () => {
  it('should create an instance', () => {
    const directive = new DataEtaDirective();
    expect(directive).toBeTruthy();
  });
});
